import { PRODUCT_SERVICE } from "./allApi";

export function* fetchProductById(id: any) {
    // let response = yield PRODUCT_SERVICE.PRODUCT_BY_ID(id);
// console.log("Product")
    // if (response.data && response.data != null) {
    //     yield put({
    //         type: ProductActions.GET_BY_SHOP_ID_ASYNC,
    //         payload: response.data
    //     });
    // } else {
    //     yield put(prepareErrorMessage(ProductActions.PRODUCT_ERROR, response));
    // }
}